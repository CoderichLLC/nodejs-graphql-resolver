{
  "name": "autograph",
  "workspaces": [
    "workspace/*"
  ],
  "scripts": {
    "publish": "npm run publish --workspaces",
    "test": "npm run test --workspaces",
    "lint": "eslint ./"
  },
  "devDependencies": {
    "@coderich/dev": "0.3.3"
  }
}
